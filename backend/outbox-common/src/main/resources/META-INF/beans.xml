<?xml version="1.0" encoding="UTF-8"?>
<!-- Marks this library as a bean archive so Quarkus indexes its classes (Jandex). The relay/purge
     base classes carry no CDI annotations themselves, but they must be in the index for ArC to
     resolve the type hierarchy of the concrete @ApplicationScoped subclasses in each service. -->
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       version="4.0" bean-discovery-mode="annotated">
</beans>

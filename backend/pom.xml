<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <groupId>chakmed.ecommerce</groupId>
  <artifactId>ecommerce-api-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <modules>
    <module>ecommerce-api-gateway</module>
    <module>products-api</module>
    <module>orders-api</module>
    <module>outbox-common</module>
    <module>products-service</module>
    <module>orders-service</module>
    <module>authenticate-service</module>
    <module>price-service</module>
    <module>featured-products-service</module>
    <module>analytics-service</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <maven-checkstyle-plugin.version>3.3.1</maven-checkstyle-plugin.version>
    <checkstyle.version>10.14.2</checkstyle.version>
    <spotbugs-maven-plugin.version>4.8.3.1</spotbugs-maven-plugin.version>
    <maven-enforcer-plugin.version>3.4.1</maven-enforcer-plugin.version>
    <jacoco-maven-plugin.version>0.8.12</jacoco-maven-plugin.version>

    <!--
      Security floors for libraries the framework BOMs still resolve below a fixed version.
      Each is the lowest release that clears the advisories Trivy reports; raise them when a
      newer advisory lands, and drop an entry once the framework BOM catches up on its own.
    -->
    <netty.version>4.1.136.Final</netty.version>
    <jackson-bom.version>2.21.4</jackson-bom.version>
    <kafka-clients.version>3.9.2</kafka-clients.version>
    <bouncycastle.version>1.81.1</bouncycastle.version>
    <postgresql.version>42.7.12</postgresql.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!--
        Plain managed versions, which do override what a module's imported BOM resolves.
        The netty and jackson floors cannot live here: an imported BOM in this parent loses to
        the framework BOM each module imports itself, so those two are imported per module,
        ahead of the framework BOM. Netty is pinned as a BOM rather than artifact by artifact
        because its modules are not designed to run at mixed versions.
      -->
      <dependency>
        <groupId>org.apache.kafka</groupId>
        <artifactId>kafka-clients</artifactId>
        <version>${kafka-clients.version}</version>
      </dependency>
      <dependency>
        <groupId>org.bouncycastle</groupId>
        <artifactId>bcprov-jdk18on</artifactId>
        <version>${bouncycastle.version}</version>
      </dependency>
      <dependency>
        <groupId>org.postgresql</groupId>
        <artifactId>postgresql</artifactId>
        <version>${postgresql.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>

      <!-- UTF-8 encoding verification -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>${maven-enforcer-plugin.version}</version>
        <executions>
          <execution>
            <id>enforce-utf8</id>
            <phase>validate</phase>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireProperty>
                  <property>project.build.sourceEncoding</property>
                  <message>project.build.sourceEncoding must be UTF-8</message>
                  <regex>UTF-8</regex>
                </requireProperty>
                <requireProperty>
                  <property>project.reporting.outputEncoding</property>
                  <message>project.reporting.outputEncoding must be UTF-8</message>
                  <regex>UTF-8</regex>
                </requireProperty>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Checkstyle linter -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>${maven-checkstyle-plugin.version}</version>
        <dependencies>
          <dependency>
            <groupId>com.puppycrawl.tools</groupId>
            <artifactId>checkstyle</artifactId>
            <version>${checkstyle.version}</version>
          </dependency>
        </dependencies>
        <executions>
          <execution>
            <id>checkstyle</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
        <configuration>
          <configLocation>${session.executionRootDirectory}/checkstyle.xml</configLocation>
          <suppressionsLocation>${session.executionRootDirectory}/checkstyle-suppressions.xml</suppressionsLocation>
          <includeTestSourceDirectory>true</includeTestSourceDirectory>
          <failOnViolation>true</failOnViolation>
          <violationSeverity>error</violationSeverity>
          <consoleOutput>true</consoleOutput>
        </configuration>
      </plugin>

      <!-- SpotBugs static analysis -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs-maven-plugin.version}</version>
        <executions>
          <execution>
            <id>spotbugs</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
        <configuration>
          <effort>Max</effort>
          <threshold>High</threshold>
          <excludeFilterFile>../spotbugs-exclude.xml</excludeFilterFile>
          <failOnError>true</failOnError>
        </configuration>
      </plugin>

      <!--
        JaCoCo coverage - 100% branch coverage on control/*Service classes and boundary resources.
        Class names are matched in dotted form: JaCoCo compares against the fully qualified name,
        so a slash-separated pattern here silently matches nothing and the gate passes everything.
      -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco-maven-plugin.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
            <configuration>
              <!--
                Surefire and Failsafe both write to this destfile. Without append the integration
                run replaces the unit run's data instead of adding to it, and the gate below then
                measures the integration tests alone - every unit test is discarded. A build always
                starts from a clean target in CI, so accumulation is bounded to one build.
              -->
              <append>true</append>
            </configuration>
          </execution>
          <execution>
            <id>check</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
            <configuration>
              <rules>
                <rule>
                  <element>CLASS</element>
                  <includes>
                    <include>the.chak.ecommerce.*.control.*Service</include>
                  </includes>
                  <limits>
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>1.0</minimum>
                    </limit>
                  </limits>
                </rule>
                <rule>
                  <element>CLASS</element>
                  <includes>
                    <include>the.chak.ecommerce.*.boundary.*Resource</include>
                    <include>the.chak.ecommerce.*.boundary.*Controller</include>
                  </includes>
                  <limits>
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>1.0</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

</project>
